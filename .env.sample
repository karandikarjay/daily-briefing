# Trusted shell syntax. Use single quotes and chmod 600 .env.
OPENAI_API_KEY='replace-with-api-key'
GOOGLE_USERNAME='sender@example.com'
PERSONAL_RECIPIENT='sender+list@example.com'
RECIPIENT_EMAILS='reader-one@example.com,reader-two@example.com'
